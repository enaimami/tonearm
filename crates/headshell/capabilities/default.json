{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: listening to events, drag and drop, and the file dialog that picks a path. No file system, shell or network permission — the dialog returns a path, and the side that reads and writes that path is the core.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}
